apiVersion: v1
kind: Service
metadata:
  labels:
    app: interventions-service
  name: interventions-service
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: interventions-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: interventions-service
  name: interventions-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: interventions-service
  template:
    metadata:
      labels:
        app: interventions-service
    spec:
      containers:
        - image: medicalife/interventions-service:latest
          name: interventions-service
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
      dnsConfig:
        nameservers:
          - 8.8.8.8
      restartPolicy: Always