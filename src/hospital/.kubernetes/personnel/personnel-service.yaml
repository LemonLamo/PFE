apiVersion: v1
kind: Service
metadata:
  labels:
    app: personnel-service
  name: personnel-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: personnel-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: personnel-service
  name: personnel-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: personnel-service
  template:
    metadata:
      labels:
        app: personnel-service
    spec:
      containers:
        - image: abdou1307/personnel-service:latest
          name: personnel-service
          ports:
            - containerPort: 8080
              protocol: TCP
      dnsConfig:
        nameservers:
          - 8.8.8.8
      restartPolicy: Always
