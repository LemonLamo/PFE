apiVersion: v1
kind: Service
metadata:
  labels:
    app: codification-db
  name: codification-db
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: codification-db
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: codification-db
  name: codification-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codification-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: codification-db
    spec:
      containers:
        - image: abdou1307/codification-db:latest
          name: codification-db
          ports:
            - containerPort: 3306
          volumeMounts:
            - mountPath: /var/lib/codification
              name: codification-data
      restartPolicy: Always
      volumes:
        - name: codification-data
          persistentVolumeClaim:
            claimName: codification-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: codification-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi