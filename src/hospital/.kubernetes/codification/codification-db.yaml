apiVersion: v1
kind: Service
metadata:
  labels:
    app: codification-db
  name: codification-db
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: codification-db
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: codification-db
  name: codification-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codification-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: codification-db
    spec:
      containers:
        - image: abdou1307/codification-db
          name: codification-db
          ports:
            - containerPort: 3306
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: mysql-data
      restartPolicy: Always
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: mysql-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi