apiVersion: v1
kind: Service
metadata:
  labels:
    app: pharmacy-service
  name: pharmacy-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: pharmacy-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: pharmacy-service
  name: pharmacy-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pharmacy-service
  template:
    metadata:
      labels:
        app: pharmacy-service
    spec:
      containers:
        - image: abdou1307/pharmacy-service:latest
          name: pharmacy-service
          ports:
            - containerPort: 8080
              protocol: TCP
      dnsConfig:
        nameservers:
          - 8.8.8.8
      restartPolicy: Always